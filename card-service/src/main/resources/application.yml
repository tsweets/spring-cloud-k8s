server:
  port: 10002

spring:
  application:
    name: CARD-SERVICE
  zipkin:
    enabled: true
    base-url: http://localhost:9411/
  jpa:
    show-sql: true

logging:
  level:
    org.hibernate.type: TRACE

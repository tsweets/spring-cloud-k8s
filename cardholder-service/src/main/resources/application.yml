server:
  port: 10001

spring:
  application:
    name: CARDHOLDER-SERVICE
  zipkin:
    enabled: true
    base-url: http://localhost:9411/
  jpa:
    show-sql: true
logging:
  level:
    org.hibernate.type: TRACE
